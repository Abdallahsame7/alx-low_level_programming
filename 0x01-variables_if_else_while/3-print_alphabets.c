#include <stdio.h>

/**
 * main - Entry point
 * Description: all char
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ch1 = 'a';
	char ch2 = 'A';

	while (ch <= 'z')
	{
	putcher(ch);
	ch++;
	}
	while (CH <= 'z')
	{
	putcher(CH);
	CH++;
	}
	putcher('\n');
	return (0);
}
