#include <stdio.h>

/**
 * main - Entry point
 * Description: easy c
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		putcher(ch);
		ch++;
	{
		pustcher('\n');
		return (0);
}
